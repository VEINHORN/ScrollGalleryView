language: android

before_script:
  - echo "sdk.dir=$ANDROID_HOME" > local.properties

android:
  components:
    - build-tools-23.0.1
    - android-23
    - extra-android-support
    - extra-android-m2repository

sudo: false

